# Release Status

This file is the short repository-owned snapshot of the current release posture for KYCortex.

## Current Snapshot

- Package version in `pyproject.toml`: `1.0.13a10.dev0`
- Latest released version: `1.0.13a6`
- Latest published release: `1.0.13a6`
- Latest published tag: `v1.0.13a6`
- Current branch for release preparation: `main`
- Current branch head is actively maintained on `main`; use repository history for the exact commit lineage.

## Coverage Status

- **Test Count**: 1967 tests (up from 1913 baseline)
- **Coverage**: 96.25% (current deterministic gate result)
- **Coverage Threshold**: 90% minimum (current: 96.25%, exceeds requirement)
- **Recent Improvements**: 
  - Fixed case-sensitivity bug in `_summary_has_active_issue()`
  - Added 39 direct unit tests for qa_tester and architect modules
  - Covered 6 additional lines in coverage improvements

## Current Posture

- The current development head is stable with comprehensive test coverage and passing CI.
- The current development head includes a feature-flagged adaptive prompt-policy core for model-aware compaction behavior (`compact`, `balanced`, `rich`) while preserving legacy defaults.
- The provider-matrix empirical workflow helpers now support configurable timeout and prompt-budget envelopes for code/test task constraints while preserving previous defaults.
- Runtime validation now applies adaptive-policy-aware secondary line-budget tolerance (`compact=0%`, `balanced=5%`, `rich=15%`) while keeping contract mismatch, syntax/import, CLI-entrypoint, and truncation failures as strict blockers.
- Documentation governance has been applied; all public/internal boundaries are respected.
- Release-candidate review remains closed pending a fresh clean canonical `15/15` real-world result.
- The latest local blocker remediation is now validated both offline against the exact persisted failed test payloads and empirically on a fresh focused rerun.
- The focused non-Ollama rerun at `output/real_world_complex_matrix_2026_04_29_head03d6403_non_ollama_blocker_literal_example_autofix_rerun` has now closed `4/4` cleanly enough for release-facing purposes: both original blocker lanes from the canonical `13/15` root (`vendor_onboarding_risk/anthropic` and `returns_abuse_screening/openai`) completed cleanly on a fresh process image, and the last control lane `returns_abuse_screening/anthropic` completed after one recovered repair cycle.
- The narrow proof rerun at `output/real_world_complex_matrix_2026_04_29_head03d6403_insurance_claim_triage_ollama_validation_failure_semantics_fix_rerun` has now closed cleanly on a fresh process image (`phase=completed`, `terminal_outcome=completed`, `repair_cycle_count=0`, `task_status_counts={done: 7}`), so the local `qa_tester` validation-failure mitigation is empirically proven rather than only locally validated.
- The currently active full-scope rerun at `output/real_world_complex_matrix_2026_04_29_head03d6403_full_scope_post_non_ollama_blocker_literal_example_autofix_rerun` then recovered `insurance_claim_triage/ollama`, completed the full `vendor_onboarding_risk/*` family, and completed `returns_abuse_screening/openai` on the same fresh process image.
- The same live root is no longer blocked on `insurance_claim_triage/ollama`; its current non-clean checkpoint is `returns_abuse_screening/anthropic`, which terminalized `degraded` at `2026-04-29T23:25:05.093925+00:00` with `repair_cycle_count=3` and `task_status_counts={done: 4, failed: 5, skipped: 4}`.
- The writer stayed alive after that degraded Anthropic lane and has now completed `returns_abuse_screening/ollama` cleanly at `2026-04-29T23:39:35.102322+00:00` with `repair_cycle_count=0` and `task_status_counts={done: 7}`.
- The same live canonical root then advanced into `access_review_audit`: `openai` completed at `2026-04-29T23:41:03.648071+00:00` with `repair_cycle_count=0`, and `anthropic` completed at `2026-04-29T23:42:40.214969+00:00` with `repair_cycle_count=0`.
- `access_review_audit/ollama` then wrote its first persisted checkpoint at `2026-04-29T23:44:47.584386+00:00`, entered repair cycle `1` after `tests` failed, and finally completed at `2026-04-30T00:05:37.695417+00:00` with `phase=completed`, `terminal_outcome=completed`, `repair_cycle_count=1`, `task_status_counts={done: 9}`, `run_result.status=completed`, and `acceptance_reason=all_evaluated_tasks_done`.
- The canonical root is now fully terminalized at `14` completed cells, `1` degraded cell, `0` active cells, and `0` unstarted cells; the only remaining non-clean checkpoint is `returns_abuse_screening/anthropic`.
- A new narrow current-head remediation is now locally validated for that sole degraded lane: `qa_tester` now recognizes positive `result.risk_factors` assertions in generated `test_risk_scoring()` cases and rewrites returns-screening payloads onto the implementation's real risk-bearing schema keys (`prior_returns`, `receipt_present`, `items`); the focused `tests/test_qa_tester.py` slice passed around the new regression and adjacent override behavior.
- The same remediation is now also proven against the exact persisted degraded artifact chain: the failed `tests` and `tests__repair_2` modules from `returns_abuse_screening/anthropic/project_state.json` both rewrite to schema-correct high-risk fixtures and both pass as repaired pytest modules against the persisted implementation artifact.
- A fresh focused rerun for `returns_abuse_screening/anthropic` on the published head `f0bbe59` has now completed cleanly at `2026-04-30T00:21:50.817929+00:00` with `phase=completed`, `terminal_outcome=completed`, `repair_cycle_count=0`, `task_status_counts={done: 7}`, and `acceptance_reason=all_evaluated_tasks_done`.
- A fresh canonical full-scope rerun is still active at `output/real_world_complex_matrix_2026_04_30_head501a3a1_full_scope_post_returns_anthropic_recovery_rerun`, but its process image started before the newest local remediation landed. It has now produced three meaningful checkpoints: `kyc_compliance_intake/openai` completed cleanly at `2026-04-30T00:26:46.806519+00:00`; `kyc_compliance_intake/anthropic` degraded at `2026-04-30T00:27:34.603554+00:00` after `tests` failed with the terse summary `Generated test validation failed: pytest failed: 1 failed, 4 passed in 0.09s` and no repair cycle opened; and `kyc_compliance_intake/ollama` is currently active after `arch`, `code`, and `deps` completed at `2026-04-30T00:34:26.437528+00:00`.
- That Anthropic terse-summary remediation is now published on `origin/main` at `8633d97`: `qa_tester` treats terse pytest-failure summaries as repairable signals for exact-status, exact-band, and positive-score overreach detection, the targeted pytest regression passes, and the exact degraded Anthropic artifact returns `True` for both `_summary_has_exact_status_action_label_assertion_issue(...)` and `_should_rebuild_from_exact_contract(...)`.
- That focused Anthropic rerun has now completed cleanly on the published remediation head at `2026-04-30T00:42:55.793394+00:00`: `phase=completed`, `terminal_outcome=completed`, `repair_cycle_count=0`, all seven canonical tasks `done`, and `acceptance_reason=all_evaluated_tasks_done`. The former terse-summary degradation is therefore empirically recovered on a fresh process image.
- The still-running pre-fix canonical root has also progressed further in parallel: `kyc_compliance_intake/ollama` completed cleanly at `2026-04-30T00:43:22.587331+00:00` with `phase=completed`, `terminal_outcome=completed`, `repair_cycle_count=0`, and all seven canonical tasks `done`; `insurance_claim_triage/openai` then completed cleanly at `2026-04-30T00:45:13.092666+00:00` with the same clean terminal shape; and `insurance_claim_triage/anthropic` is now the live frontier at `2026-04-30T00:46:19.483720+00:00` with `arch`, `code`, `deps`, `tests`, `review`, and `docs` done while only `legal` remains pending.
- No canary claim or production-readiness claim is attached to the current head.
- Repository is in excellent operational state for package-level release review.
- Current-head Phase 16 material is now tracked as a pre-canary evidence rebuild, not as an open or closed canary window.
- Historical canary operations and evidence are retained separately and are not summarized here.

## Repository Release Gate

- Deterministic repository release validation is currently green on the development head.
- The latest validated gate includes `ruff`, `mypy`, focused public and package regressions, package validation, release metadata validation, the repository coverage gate, and the full pytest suite.
- The latest deterministic revalidation checkpoint (`2026-04-28`) remained green on the current `main` head with `1967` tests passing and coverage gate passing at `96.25%`.
- The latest canonical real-world 5x3 campaign progression:
  - Ronda 4: 10/15 clean (generated 2026-04-27T03:34:04Z)
  - Ronda 5: 11/15 clean (generated 2026-04-27T04:25:49Z, +1 improvement)
  - Candidate review remains closed (need 15/15 to open)
- **Current active-scope evidence**: the latest `openai + ollama` compatibility snapshot is `10/10` clean (`7/7` tasks done per provider) in `output/real_world_complex_matrix_2026_04_28_full_scope/campaign_summary.json`.
- Fresh isolated current-head requalification at `output/real_world_complex_matrix_2026_04_28_head03d6403_rc_requal` completed `kyc_compliance_intake/openai` cleanly but failed immediately on `kyc_compliance_intake/anthropic`; persisted current-head state records provider rejection, `workflow_blocked`, and exhausted repair budget.
- Fresh current-environment Anthropic preflight is healthy again on the configured Anthropic-compatible gateway, so the remaining release-candidate gate is now a fresh full-scope empirical rerun rather than a known preflight blocker.
- The most recent post-thaw current-head requalification currently stands at `output/real_world_complex_matrix_2026_04_29_head03d6403_rc_requal_thawed`; `kyc_compliance_intake/openai`, `kyc_compliance_intake/anthropic`, `insurance_claim_triage/openai`, and `insurance_claim_triage/anthropic` have all completed cleanly there.
- The first post-thaw Ollama scenario (`kyc_compliance_intake/ollama`) has now terminalized its repair budget: `tests__repair_3` also failed (`pytest failed: 1 failed, 4 passed in 0.10s`), the persisted scenario state remains `phase=failed`, `terminal_outcome=failed`, and `repair_cycle_count=3`, with the latest persisted write at `2026-04-29T00:30:39.817274+00:00`.
- Failure policy `continue` then advanced the same root beyond `insurance_claim_triage/ollama`, which completed cleanly at `2026-04-29T01:17:32.154579+00:00` after one recovered code repair cycle. The same root then completed `vendor_onboarding_risk/openai` cleanly at `2026-04-29T01:18:50.552353+00:00`, completed `vendor_onboarding_risk/anthropic` in `phase=completed` with `terminal_outcome=degraded`, `repair_cycle_count=2`, and `updated_at=2026-04-29T01:21:27.177170+00:00`, and then widened the `vendor_onboarding_risk/ollama` generated-test lane once more: at the historical failed checkpoint `2026-04-29T02:13:54.273606+00:00`, the scenario sat at `phase=failed`, `terminal_outcome=failed`, and `repair_cycle_count=3`, with derived task state `done=4`, `failed=3`, `pending=4`; `tests__repair_2` failed with a three-failure pytest run (`test_happy_path`, `test_validation_failure`, and `test_batch_processing`), and `tests__repair_3` remained pending in the state file. Two pre-fix resume attempts then contaminated the same post-thaw root: a whole-root relaunch rewrote `kyc_compliance_intake/openai`, and a scenario-scoped relaunch rewrote `vendor_onboarding_risk/ollama` to a fresh `arch` checkpoint (`updated_at=2026-04-29T02:25:22.749396+00:00`, `repair_cycle_count=0`). `scripts/run_real_world_complex_matrix.py` is now patched and locally validated to reuse cached terminal results and load persisted failed/interrupted state on future resume-mode launches, but the current post-thaw root can no longer serve as an authentic source for the historical `tests__repair_2` vendor failure snapshot.
- The clean post-fix vendor-only recovery root at `output/real_world_complex_matrix_2026_04_29_head03d6403_vendor_ollama_postfix_recovery` has now terminalized as a closed old-runtime failure artifact. Its final persisted checkpoint landed at `2026-04-29T03:09:34.506579+00:00` with `phase=failed`, `terminal_outcome=failed`, `failure_category=workflow_blocked`, `repair_cycle_count=3`, and derived task state `done=1`, `failed=4`, `pending=5`. `code`, `code__repair_1`, `code__repair_2`, and `code__repair_3` all repeated the same `NameError: name 'dataclass' is not defined`, and the writer exited with scenario summary `status=execution_error phase=failed terminal=failed`. Because that process started before the local validation-runtime autofix landed, the next release-facing empirical step is a fresh rerun under the current codebase rather than another interpretation of this closed old-runtime chain.
- That fresh vendor-only rerun has now terminalized at `output/real_world_complex_matrix_2026_04_29_head03d6403_vendor_ollama_runtime_autofix_rerun`; its terminal persisted checkpoint landed at `2026-04-29T03:47:18.018534+00:00` with `phase=completed`, `terminal_outcome=degraded`, `failure_category=scenario_validation`, `repair_cycle_count=0`, and derived task state `done=7`. All canonical tasks completed and the rerun never opened any repair lane.
- The persisted fresh-run artifacts now include `code_implementation.py`, `tests_tests.py`, `review_review.md`, `docs_documentation.md`, and `legal_legal_analysis.md`; `code_implementation.py` begins with `from dataclasses import dataclass`, and separate direct local validation had already confirmed that the exact historical failed code artifact from the closed old root passes the current `validation_runtime.py` autofix path. The old `NameError` code-validation defect is therefore empirically cleared on the fresh process, but the rerun still closed `degraded` because `run_result.json` reports `acceptance_reason=productivity_validation_failed` and scenario-validation error `Generated code did not explicitly reject a malformed request through handle_request(...)`.
- A follow-up fresh vendor-only rerun under additional malformed-request prompt/tests hardening has now terminalized at `output/real_world_complex_matrix_2026_04_29_head03d6403_vendor_ollama_malformed_request_prompt_rerun`; its terminal persisted checkpoint landed at `2026-04-29T04:30:25.694403+00:00` with `phase=completed`, `terminal_outcome=completed`, `failure_category=null`, and derived task state `done=7`. The persisted `code_implementation.py` explicitly raises `ValueError("Invalid request details")` for malformed non-dict `details`, so the malformed-request hardening is empirically visible in generated code and the workflow now converges through all canonical tasks on a fresh process image.
- That completed root is not yet acceptance evidence for the latest validator slice: the generated `tests_tests.py` artifact remains permissive (`details=None` with a tolerant `try/except ValueError` fallback), and a direct local probe under the current `_validate_generated_scenario(...)` now rejects the root with `Generated tests did not explicitly assert ValueError for malformed non-dict details.` The next release-facing empirical step is therefore another fresh rerun under the new validator image, not reuse of this completed root.
- That next fresh vendor-only rerun is now active at `output/real_world_complex_matrix_2026_04_29_head03d6403_vendor_ollama_generated_test_validator_rerun`; its first persisted checkpoint landed at `2026-04-29T04:39:30.539084+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and derived task state `done=1`, `pending=6`. `arch` is done, `code` is live, and only `arch_architecture.md` exists so far, so the next checkpoint remains the first discriminating evidence for the validator-aware rerun.
- The same validator-aware rerun has now advanced to `2026-04-29T04:50:53.237700+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and derived task state `done=2`, `pending=5`. `arch` and `code` are done, `deps` is live, and the persisted `code_implementation.py` still explicitly raises `ValueError("Invalid request details")`, so the improved malformed-request code path remains stable under the new validator-aware run.
- The same validator-aware rerun has now advanced again to `2026-04-29T04:51:31.885520+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and derived task state `done=3`, `pending=4`. `arch`, `code`, and `deps` are done, `tests` is live, and the artifact set now includes `requirements.txt`, so the validator-aware run is still repair-free through the entire pre-tests path.
- The same validator-aware rerun has now advanced again to `2026-04-29T05:01:33.741905+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and derived task state `done=4`, `pending=3`. `arch`, `code`, `deps`, and `tests` are done, `review` is live, and the artifact set now includes `tests_tests.py`; that generated test artifact is still permissive, and a direct local probe of the active root under the current validator already fails with `Generated tests did not explicitly assert ValueError for malformed non-dict details.`
- The same validator-aware rerun has now advanced again to `2026-04-29T05:07:59.846622+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and derived task state `done=6`, `pending=1`. `review` and `docs` are now done, `legal` is live, and the direct local validator probe still predicts the same terminal rejection once the workflow finishes.
- That validator-aware rerun has now terminalized at `2026-04-29T05:11:21.308805+00:00` with `phase=completed`, `terminal_outcome=degraded`, `failure_category=scenario_validation`, and derived task state `done=7`. Its `run_result.json` exactly matches the earlier direct probe: `status=validation_error`, `acceptance_reason=productivity_validation_failed`, and scenario-validation error `Generated tests did not explicitly assert ValueError for malformed non-dict details.`
- The nearest local rewrite path has now been fixed and locally validated: `kycortex_agents/agents/qa_tester.py` no longer rewrites `with pytest.raises(ValueError)` in `test_validation_failure` when the implementation already raises `ValueError`, the focused `tests/test_qa_tester.py` slice passed, and a direct probe against the real terminal implementation preserved the explicit malformed-request assertion.
- A fresh vendor-only rerun under that QA tester fix is now live at `output/real_world_complex_matrix_2026_04_29_head03d6403_vendor_ollama_qa_tester_value_error_preservation_rerun`. Launch evidence is limited so far to writer terminal `22761347-d51e-426f-a6f3-100b03318b4a` reaching `[INFO] workflow_started` and `[INFO] task_started`; the first persisted checkpoint has not been written yet.
- The same QA-tester-preservation rerun has now written its first persisted checkpoint at `2026-04-29T05:20:42.763595+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and derived task state `done=1`, `pending=6`. `arch` is done, `code` is live, and only `arch_architecture.md` exists so far.
- The same QA-tester-preservation rerun has now advanced again to `2026-04-29T05:32:06.328743+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and derived task state `done=2`, `pending=5`. `arch` and `code` are done, `deps` is live, and the artifact set now includes `code_implementation.py`.
- The same QA-tester-preservation rerun has now advanced again to `2026-04-29T05:42:48.740933+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and derived task state `done=4`, `pending=3`. `arch`, `code`, `deps`, and `tests` are done, `review` is live, and the artifact set now includes `tests_tests.py`. That generated test artifact now preserves the explicit malformed-request `with pytest.raises(ValueError)` assertion, and a direct local validator probe of the active root now returns `validated=True`.
- The same QA-tester-preservation rerun has now advanced again to `2026-04-29T05:48:49.137408+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and derived task state `done=6`, `pending=1`. `review` and `docs` are now done, `legal` is live, and the direct local validator probe remains green.
- That QA-tester-preservation rerun has now terminalized at `2026-04-29T05:52:11.957915+00:00` with `phase=completed`, `terminal_outcome=completed`, `failure_category=null`, and derived task state `done=7`. Its `run_result.json` reports `status=completed`, `acceptance_reason=all_evaluated_tasks_done`, and a fully green scenario-validation block, restoring fresh vendor-only repository evidence for this blocker slice.
- A fresh three-provider preflight at `2026-04-29T09:47:42Z` returned `healthy` for `openai`, `anthropic`, and `ollama`, so the broader canonical rerun is now active on the current host rather than merely planned.
- That fresh full-scope rerun is live at `output/real_world_complex_matrix_2026_04_29_head03d6403_full_scope_post_qa_tester_fix`.
- `kyc_compliance_intake/openai` recovered through `tests__repair_1` and terminalized `completed` at `2026-04-29T09:50:15.923818+00:00` with `repair_cycle_count=1` and `task_status_counts={done: 8}`.
- `kyc_compliance_intake/anthropic` then terminalized `completed` at `2026-04-29T09:51:56.288283+00:00` with `repair_cycle_count=0` and `task_status_counts={done: 7}`.
- `kyc_compliance_intake/ollama` then terminalized `completed` at `2026-04-29T10:07:09.122857+00:00` with `repair_cycle_count=0`, `task_status_counts={done: 7}`, and `acceptance_reason=all_evaluated_tasks_done`.
- `insurance_claim_triage/openai` then terminalized `completed` at `2026-04-29T10:08:36.051610+00:00` with `repair_cycle_count=0`, `task_status_counts={done: 7}`, and `acceptance_reason=all_evaluated_tasks_done`.
- `insurance_claim_triage/anthropic` then terminalized `completed` at `2026-04-29T10:10:10.650539+00:00` with `repair_cycle_count=0`, `task_status_counts={done: 7}`, and `acceptance_reason=all_evaluated_tasks_done`.
- `insurance_claim_triage/ollama` then terminalized `completed` at `2026-04-29T10:24:17.276905+00:00` with `repair_cycle_count=0`, `task_status_counts={done: 7}`, and `acceptance_reason=all_evaluated_tasks_done`.
- `vendor_onboarding_risk/openai` then terminalized `completed` at `2026-04-29T10:25:40.951172+00:00` with `repair_cycle_count=0`, `task_status_counts={done: 7}`, and `acceptance_reason=all_evaluated_tasks_done`.
- `vendor_onboarding_risk/anthropic` then terminalized `completed` at `2026-04-29T10:27:14.801490+00:00` with `repair_cycle_count=0`, `task_status_counts={done: 7}`, and `acceptance_reason=all_evaluated_tasks_done`.
- `vendor_onboarding_risk/ollama` then terminalized `completed` at `2026-04-29T10:43:00.668466+00:00` with `repair_cycle_count=0`, `task_status_counts={done: 7}`, and `acceptance_reason=all_evaluated_tasks_done`.
- `returns_abuse_screening/openai` then terminalized `completed` at `2026-04-29T10:44:31.330527+00:00` with `repair_cycle_count=0`, `task_status_counts={done: 7}`, and `acceptance_reason=all_evaluated_tasks_done`.
- `returns_abuse_screening/anthropic` then terminalized `completed` at `2026-04-29T10:46:22.598633+00:00` with `repair_cycle_count=0`, `task_status_counts={done: 7}`, and `acceptance_reason=all_evaluated_tasks_done`.
- `returns_abuse_screening/ollama` then terminalized `completed` at `2026-04-29T11:01:02.223726+00:00` with `repair_cycle_count=0`, `task_status_counts={done: 7}`, and `acceptance_reason=all_evaluated_tasks_done`.
- `access_review_audit/openai` then terminalized `completed` at `2026-04-29T11:02:32.767195+00:00` with `repair_cycle_count=0`, `task_status_counts={done: 7}`, and `acceptance_reason=all_evaluated_tasks_done`.
- `access_review_audit/anthropic` then terminalized `completed` at `2026-04-29T11:04:14.614927+00:00` with `repair_cycle_count=0`, `task_status_counts={done: 7}`, and `acceptance_reason=all_evaluated_tasks_done`.
- The canonical rerun then terminalized non-clean at `access_review_audit/ollama`: `phase=failed`, `terminal_outcome=failed`, `repair_cycle_count=3`, `task_status_counts={done: 1, failed: 4, pending: 5}`, and `run_result.json` reported `status=execution_error` with `acceptance_reason=evaluated_tasks_incomplete`.
- The blocking defect is narrow and repeated rather than a provider-health outage: `code`, `code__repair_1`, `code__repair_2`, and `code__repair_3` all failed `code_validation` with `Generated code validation failed: module import failed: NameError: name 'List' is not defined. Did you mean: 'list'?` while provider calls remained healthy.
- A local mitigation is already validated on the current head: `kycortex_agents/orchestration/validation_runtime.py` now auto-injects missing `typing` aliases used in annotations before import validation; focused support tests passed, and the exact failed `access_review_audit/ollama` artifact now imports cleanly under a direct local probe.
- The first clean scenario-scoped verification rerun under the typing-import autofix cleared the former missing-`List` code-validation blocker on a clean root, but it later failed again downstream in `tests__repair_1` at `2026-04-29T11:51:12.284794+00:00`. The persisted failure showed a repair-instruction blind spot: warning-only test repair guidance highlighted only `details=None` while omitting the real pytest failure details for invalid happy-path fixture payloads.
- A second narrow remediation is now validated on the current head: `kycortex_agents/orchestration/repair_instructions.py` now preserves actual pytest failure details and payload-contract violations when warning-only `test_validation` repairs are generated. The focused `build_repair_instruction` pytest slice passed, but fresh empirical evidence is still required because the earlier live rerun started before this fix landed.
- The fresh scenario-scoped rerun under the first two local mitigations later exhausted its repair budget on a stale process image: its final persisted checkpoint landed at `2026-04-29T12:25:16.275890+00:00` in `output/real_world_complex_matrix_2026_04_29_head03d6403_access_review_audit_ollama_typing_import_and_test_repair_fix_rerun` with `phase=failed`, `terminal_outcome=failed`, `repair_cycle_count=3`, and derived task state `done=5`, `failed=3`, `pending=5`. The live chain had already proved the warning-only repair-instruction fix was loaded, but it still failed downstream on nested invalid request payloads in generated tests.
- A third local repo fix is now validated locally: `dict_accessed_keys_from_tree()` recognizes dict keys seen through both `in` and `not in` membership checks, and `auto_fix_test_type_mismatches()` now uses those inferred nested keys to normalize placeholder payloads before pytest execution. The exact stale `tests__repair_1` artifact now rewrites `approval_metadata={'key': 'sample'}` into a valid nested dict under the current codebase, so the release gate remains closed only pending fresh empirical proof on a new process image.
- That fresh proof path is now active: the new scenario-scoped rerun at `output/real_world_complex_matrix_2026_04_29_head03d6403_access_review_audit_ollama_typing_import_test_repair_and_nested_payload_autofix_rerun` has already cleared `code` at `2026-04-29T12:41:49.387699+00:00` with `phase=execution`, `terminal_outcome=null`, and `repair_cycle_count=0`. The former missing-`List` blocker family remains absent on this newest process image, and `deps` is now the live frontier.
- The same fresh process then completed `deps` and failed the initial `tests` task at `2026-04-29T12:45:07.766992+00:00`. The failing pytest module still uses `details='source'` in happy-path and batch cases, so this checkpoint does not yet exercise the third local mitigation; it only reopens repair cycle `1` on the new process image. The release gate therefore remains closed, with `tests__repair_1` now the next discriminating frontier.
- Repair cycle `1` has since advanced to `2026-04-29T12:50:33.229099+00:00` with `code__repair_1=done`, `tests__repair_1` live, `phase=execution`, and `terminal_outcome=null`. The former code blocker still remains closed, so the next release-facing question is whether the first repaired pytest artifact now converges under the third local mitigation.
- That repaired pytest lane has now converged at `2026-04-29T12:54:23.833943+00:00`: `tests` and `tests__repair_1` are both `done`, `repair_cycle_count=1`, and the fresh process remains in `phase=execution` with `terminal_outcome=null`. The repaired artifact switched the failing string payloads to direct dict literals and passed, but it did not reproduce the older nested placeholder shape, so the third local mitigation remains locally validated rather than directly exercised by this run.
- The same run has since cleared `review` and `docs` at `2026-04-29T12:56:59.166012+00:00`, leaving only `legal` pending with `phase=execution`, `terminal_outcome=null`, and `repair_cycle_count=1`. The release gate therefore remains closed only pending the final downstream legal result and overall workflow outcome on this fresh process image.
- The fresh scenario-scoped rerun at `output/real_world_complex_matrix_2026_04_29_head03d6403_access_review_audit_ollama_typing_import_test_repair_and_nested_payload_autofix_rerun` is now complete at `2026-04-29T12:58:02.956548+00:00`: `phase=completed`, `terminal_outcome=completed`, `repair_cycle_count=1`, and `task_status_counts={done: 9}`. The narrow `access_review_audit/ollama` blocker is therefore closed on a fresh process image.
- That repaired rerun still did not directly reproduce the older nested placeholder payload shape that motivated the third local mitigation, because the successful repaired pytest artifact converged with direct dict payloads instead. The third mitigation remains locally validated, but not directly exercised by this live run.
- The release gate remains closed because the fresh canonical rerun at `output/real_world_complex_matrix_2026_04_29_head03d6403_full_scope_post_access_review_audit_ollama_three_fix_rerun` finished non-clean at `12/15` rather than reopening release-candidate review.
- The service did not block: terminal `faddfe69-8cf8-412e-9658-6ad69c3fcd10` exited `0`, every `openai` cell completed, every `anthropic` cell completed, `returns_abuse_screening/ollama` completed, and `access_review_audit/ollama` completed at `2026-04-29T15:59:50.764865+00:00` with `repair_cycle_count=1`.
- The remaining canonical blockers are now `kyc_compliance_intake/ollama`, `insurance_claim_triage/ollama`, and `vendor_onboarding_risk/ollama`, each terminalized `failed` after exhausting `3` repair cycles in the `tests` lane.
- `insurance_claim_triage/ollama` is the highest-signal current anchor because its final persisted error explicitly cites missing request payload fields `policy_id`, `claim_category`, `claim_amount`, and `evidence`, which points to a likely shared generated-test payload defect rather than provider unavailability.
- A shared local remediation is now validated narrowly and against the exact persisted failed artifacts: the test autofix now lifts `required_fields` validators onto dict payload names before rebuilding positive-path fixtures, and temporary local pytest replays of all three persisted `tests__repair_3` artifacts now pass.
- A fresh focused Ollama-only rerun has now completed at `output/real_world_complex_matrix_2026_04_29_head03d6403_ollama_three_remaining_required_fields_autofix_rerun` in terminal `843d6f40-a476-41a8-a2ed-806c6c1751af` for `kyc_compliance_intake`, `insurance_claim_triage`, and `vendor_onboarding_risk`; the terminal exited `0` and emitted `campaign_summary.json` plus `campaign_summary.md`.
- The active focused rerun has now fully recovered the first of the three canonical Ollama blockers on a fresh process image: `kyc_compliance_intake/ollama` completed at `2026-04-29T18:55:06.614122+00:00` with `repair_cycle_count=0`, `task_status_counts={done: 7}`, `run_result.status=completed`, and `acceptance_reason=all_evaluated_tasks_done`.
- The same focused writer has now also fully recovered `insurance_claim_triage/ollama` on a fresh process image: it completed at `2026-04-29T19:09:11.386074+00:00` with `repair_cycle_count=0`, `task_status_counts={done: 7}`, `run_result.status=completed`, and `acceptance_reason=all_evaluated_tasks_done`.
- The third and last focused scenario, `vendor_onboarding_risk/ollama`, then completed at `2026-04-29T19:25:20.307372+00:00` with `phase=completed`, `terminal_outcome=completed`, `repair_cycle_count=0`, `task_status_counts={done: 7}`, and `legal` completed at `2026-04-29T19:25:20.277421+00:00`.
- The focused rerun therefore closed `3/3` previously failed canonical Ollama scenarios cleanly on a fresh process image, materially upgrading release confidence in the shared required-fields autofix while leaving the canonical full-scope root itself still at `12/15` until re-run.
- After re-verifying the USB backup gate at `2026-04-29 20:30 WEST` (`active (waiting)` timer, latest successful run `2026-04-29 20:18:05 WEST`), a fresh canonical full-scope rerun was launched in terminal `99772377-d519-4a2b-9dad-00d1c4023ecc` at `output/real_world_complex_matrix_2026_04_29_head03d6403_full_scope_post_required_fields_autofix_rerun`.
- That new root then completed `returns_abuse_screening/ollama` cleanly at `2026-04-29T20:44:51.350819+00:00`, completed `access_review_audit/openai` at `2026-04-29T20:46:35.072540+00:00`, completed `access_review_audit/anthropic` at `2026-04-29T20:48:18.845123+00:00`, and closed `access_review_audit/ollama` at `2026-04-29T21:11:06.935887+00:00` after one repair cycle with `task_status_counts={done: 9}`. The full current-head canonical rerun therefore terminalized at `13/15`: every Ollama cell completed, but `vendor_onboarding_risk/anthropic` still failed with `run_result.status=execution_error` and `returns_abuse_screening/openai` still failed with `run_result.status=execution_error`.
- The latest local mitigation then proved the exact persisted failed test payloads on both blocker roots can now be normalized offline under the current AST autofix: vendor onboarding happy-path placeholders are replaced with concrete implementation literals, and returns screening happy-path `items` placeholders are replaced with a concrete list-of-dicts payload.
- A fresh focused non-Ollama rerun then turned that local proof into empirical evidence: `vendor_onboarding_risk/anthropic` completed cleanly at `2026-04-29T21:59:56.028884+00:00`, `returns_abuse_screening/openai` completed cleanly at `2026-04-29T22:01:13.077001+00:00`, and the bundle closed `4/4` with zero execution errors.
- Full release-candidate review therefore remains closed until the fresh canonical root is recovered from `13/15` to a clean `15/15` on the current head.
- A green deterministic gate does not by itself create a new release or production claim.

## Next Release-Facing Action

1. Publish the canonical `insurance_claim_triage/openai` completion plus the active `insurance_claim_triage/anthropic` legal-tail checkpoint.
2. Continue monitoring the still-running canonical root from `insurance_claim_triage/anthropic`.
3. Keep release-candidate review closed until a post-fix canonical rerun proves `15/15` on the current head.


## Canonical References

- [RELEASE.md](RELEASE.md)
- [CHANGELOG.md](CHANGELOG.md)
- [MIGRATION.md](MIGRATION.md)
- [docs/go-live-policy.md](docs/go-live-policy.md)