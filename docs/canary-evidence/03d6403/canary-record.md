# Canary Record - 03d6403

Status: pre-canary evidence rebuild; live window not started

This record opens the candidate evidence bundle for commit `03d6403b58e5e2365501b144f50f90976dc1311b`.

It does not claim that the canary window has started or closed.

This bundle exists because the original late-phase local evidence chain was lost and the current head now needs a repository-controlled replacement record before any new Phase 16 decision can be made.

## Candidate Identity

- candidate commit SHA: `03d6403b58e5e2365501b144f50f90976dc1311b`
- candidate commit subject: `fix(pylance): restore non-strict diagnostics with explicit python runtime`
- branch: `main`
- package version: `1.0.13a10.dev0`
- signature state: local current head, not tagged for release
- deterministic repository gate: green on the current head (`scripts/release_metadata_check.py`, `scripts/release_check.py`, and the full repository test gate)
- release-candidate baseline run: still closed pending a fresh full-scope `15/15` result on this exact head

## Current Owner Binding

| Role | Current named owner | Contact path |
| --- | --- | --- |
| Release owner | Alexandre Andrade | `alex@kycortex.com` |
| Canary operator | Alexandre Andrade | `alex@kycortex.com` |
| Support responder | Alexandre Andrade | `alex@kycortex.com` |
| Security responder | Alexandre Andrade | `alex@kycortex.com` |

## Rollback Target

- rollback target SHA: `f99a38d1a08dceedcd0b520e302e3615f81d60f0`
- rollback target tag: `v1.0.13a6`
- rollback target role: latest published known-good line with retained Phase 16 evidence
- rollback smoke status for the live canary environment: not activated on the current head because the live window has not started

## Canary Scope

- deployment class: single-maintainer pre-production canary
- environment identifier: live canary environment not admitted yet; current pre-canary evidence comes from repository-owned outputs under `output/real_world_complex_matrix_2026_04_28_full_scope/` and `output/real_world_complex_matrix_2026_04_28_head03d6403_rc_requal/`
- eligible workflow classes: not yet admitted; current evidence uses repository-owned empirical runner outputs only
- canary start time: not started
- minimum evidence window: at least 7 consecutive days or 100 eligible workflows, whichever is later

## Current State

- the canary window has not started yet
- the current head has a clean active-scope empirical baseline for `openai + ollama` at `10/10` in `output/real_world_complex_matrix_2026_04_28_full_scope/campaign_summary.json`
- the fresh isolated full-scope current-head rerun in `output/real_world_complex_matrix_2026_04_28_head03d6403_rc_requal/` completed `kyc_compliance_intake/openai` cleanly on the current head
- the same current-head rerun then failed immediately on `kyc_compliance_intake/anthropic`; persisted state records `Architect: Anthropic provider rejected the model API request`, provider-health status `failing`, three exhausted repair cycles, and final `workflow_blocked`
- a fresh current-environment pre-canary health probe on the configured Anthropic-compatible gateway is now healthy again: both model inventory and the minimal live message probe succeeded at `2026-04-28T23:05:00Z`
- a fresh current-environment pre-canary health probe at `2026-04-29T09:47:42Z` returned `healthy` for `openai`, `anthropic`, and `ollama` on the maintainer host
- the fresh full-scope `15/15` current-head rerun is now live at `output/real_world_complex_matrix_2026_04_29_head03d6403_full_scope_post_qa_tester_fix`
- `kyc_compliance_intake/openai` recovered through `tests__repair_1` and terminalized `completed` at `2026-04-29T09:50:15.923818+00:00` with `repair_cycle_count=1`
- `kyc_compliance_intake/anthropic` then terminalized `completed` at `2026-04-29T09:51:56.288283+00:00` with `repair_cycle_count=0`
- `kyc_compliance_intake/ollama` then terminalized `completed` at `2026-04-29T10:07:09.122857+00:00` with `repair_cycle_count=0`
- `insurance_claim_triage/openai` then terminalized `completed` at `2026-04-29T10:08:36.051610+00:00` with `repair_cycle_count=0`
- `insurance_claim_triage/anthropic` then terminalized `completed` at `2026-04-29T10:10:10.650539+00:00` with `repair_cycle_count=0`
- `insurance_claim_triage/ollama` then terminalized `completed` at `2026-04-29T10:24:17.276905+00:00` with `repair_cycle_count=0`
- `vendor_onboarding_risk/openai` then terminalized `completed` at `2026-04-29T10:25:40.951172+00:00` with `repair_cycle_count=0`
- `vendor_onboarding_risk/anthropic` then terminalized `completed` at `2026-04-29T10:27:14.801490+00:00` with `repair_cycle_count=0`
- `vendor_onboarding_risk/ollama` then terminalized `completed` at `2026-04-29T10:43:00.668466+00:00` with `repair_cycle_count=0`
- `returns_abuse_screening/openai` then terminalized `completed` at `2026-04-29T10:44:31.330527+00:00` with `repair_cycle_count=0`
- `returns_abuse_screening/anthropic` then terminalized `completed` at `2026-04-29T10:46:22.598633+00:00` with `repair_cycle_count=0`
- `returns_abuse_screening/ollama` then terminalized `completed` at `2026-04-29T11:01:02.223726+00:00` with `repair_cycle_count=0`
- `access_review_audit/openai` then terminalized `completed` at `2026-04-29T11:02:32.767195+00:00` with `repair_cycle_count=0`
- `access_review_audit/anthropic` then terminalized `completed` at `2026-04-29T11:04:14.614927+00:00` with `repair_cycle_count=0`
- the canonical rerun then terminalized non-clean at `access_review_audit/ollama`, which failed at `2026-04-29T11:19:33.647158+00:00` after exhausting `3` repair cycles on a repeated missing-`List` `code_validation` defect
- a narrow current-head mitigation is already validated locally: the validation runtime now auto-injects missing `typing` aliases used in annotations before import execution, and the exact failed artifact imports cleanly under a direct probe after injecting `from typing import List`
- a clean scenario-scoped verification rerun is now active at `output/real_world_complex_matrix_2026_04_29_head03d6403_access_review_audit_ollama_typing_import_autofix_rerun`; its first persisted checkpoint landed at `2026-04-29T11:34:13.599385+00:00` with `arch` done and `code` live
- that clean verification rerun then cleared the former blocker lane at `2026-04-29T11:38:39.187256+00:00`: `arch` and `code` are both `done`, `code_implementation.py` is persisted, `repair_cycle_count` remains `0`, and `deps` is now live
- the same clean verification rerun then shifted to a new downstream blocker at `2026-04-29T11:41:56.900646+00:00`: `tests` failed validation, `review/docs/legal` were skipped, `terminal_outcome` became `degraded`, and repair cycle `1` opened with `code__repair_1` plus `tests__repair_1` pending
- repair cycle `1` then advanced at `2026-04-29T11:47:22.638263+00:00`: `code__repair_1` completed, `tests__repair_1` remained live, `phase` returned to `execution`, and `terminal_outcome` returned to `null`
- the same clean verification rerun then failed `tests__repair_1` at `2026-04-29T11:51:12.284794+00:00`; the persisted failure still reflected invalid fixture payload shape in generated tests, and the repair cycle `2` writer was later terminated because a narrower repo fix landed that the old process image could not consume
- a second local mitigation is now validated: warning-only test-repair instructions preserve real pytest failure details and payload-contract violations, and the next empirical frontier is a fresh rerun started only after that fix landed
- that fresh rerun under both local mitigations has now written its first persisted checkpoint at `2026-04-29T11:58:23.634429+00:00`: `arch` is done, `code` is live, `repair_cycle_count` remains `0`, and no repair lane has opened yet
- that same fresh rerun then cleared `code` at `2026-04-29T12:02:50.106062+00:00`: `arch` and `code` are both `done`, `code_implementation.py` is persisted, `repair_cycle_count` remains `0`, and `deps` is now live
- that same fresh rerun then failed `tests` at `2026-04-29T12:06:07.807439+00:00`, but unlike the stale earlier process its repair context now includes the real pytest failure detail inside the warning-only repair instruction; `tests__repair_1` is now the live frontier
- repair cycle `1` then advanced at `2026-04-29T12:11:34.360572+00:00`: `code__repair_1` completed, `tests__repair_1` remained live, and the fresh process stayed in `phase=execution` with `terminal_outcome=null`
- that same fresh rerun later failed `tests__repair_1`, failed `tests__repair_2`, and terminalized at `2026-04-29T12:25:16.275890+00:00` with `phase=failed`, `terminal_outcome=failed`, and `repair_cycle_count=3`; it is now stale evidence only because the process image cannot consume new repo code
- a third local mitigation is now validated locally at `2026-04-29T12:29:00Z`: dict key extraction now recognizes both `in` and `not in` membership checks, and the test autofix now rewrites nested placeholder payloads like `approval_metadata={'key': 'sample'}` into concrete fixtures on the exact stale failed artifact; the next empirical frontier is a fresh rerun started after this fix
- that fresh three-fix rerun is now launched at `output/real_world_complex_matrix_2026_04_29_head03d6403_access_review_audit_ollama_typing_import_test_repair_and_nested_payload_autofix_rerun`; launch evidence is currently limited to writer terminal `8a47678c-faaa-46ed-a250-37e022c022b2` reaching `[INFO] workflow_started` and `[INFO] task_started`, so the first persisted checkpoint is still pending
- that same three-fix rerun then wrote its first persisted checkpoint at `2026-04-29T12:37:22.748392+00:00`: `arch` is done, `code` is live, `repair_cycle_count` remains `0`, and the active frontier has moved to the first persisted `code` lane on the new process image
- that same three-fix rerun then cleared `code` at `2026-04-29T12:41:49.387699+00:00`: `arch` and `code` are both `done`, `code_implementation.py` is persisted, `repair_cycle_count` remains `0`, and `deps` is now the live frontier
- that same three-fix rerun then completed `deps` and failed `tests` at `2026-04-29T12:45:07.766992+00:00`; the initial failing test artifact still uses `details='source'`, so repair cycle `1` is now the first live frontier where the nested-payload autofix can actually be exercised
- repair cycle `1` then advanced at `2026-04-29T12:50:33.229099+00:00`: `code__repair_1` completed, `tests__repair_1` remained live, and the fresh process stayed in `phase=execution` with `terminal_outcome=null`
- repair cycle `1` then cleared the repaired pytest lane at `2026-04-29T12:54:23.833943+00:00`: both `tests` and `tests__repair_1` are `done`, the live artifact converged by using direct dict payloads, and the remaining frontier moved downstream to `review`, `docs`, and `legal`
- the same three-fix rerun then cleared `review` and `docs` at `2026-04-29T12:56:59.166012+00:00`, leaving only `legal` pending on the scenario-scoped run
- the same three-fix rerun then cleared `legal` at `2026-04-29T12:58:02.917393+00:00`, leaving all tasks complete while the final workflow outcome write was still pending
- the same three-fix rerun then wrote its authoritative terminal state at `2026-04-29T12:58:02.956548+00:00` with `phase=completed`, `terminal_outcome=completed`, and `repair_cycle_count=1`; the narrow `access_review_audit/ollama` blocker is now closed on a fresh process image, though this run still did not directly reproduce the older nested placeholder payload shape
- after that narrow rerun completed, a fresh canonical full-scope rerun was launched at `output/real_world_complex_matrix_2026_04_29_head03d6403_full_scope_post_access_review_audit_ollama_three_fix_rerun`; it later finished with `12/15` clean cells overall
- the fresh canonical rerun closed the former `access_review_audit/ollama` blocker on a fresh process image: `access_review_audit/ollama` completed at `2026-04-29T15:59:50.764865+00:00` with `repair_cycle_count=1`
- the same canonical rerun still remains non-clean overall because `kyc_compliance_intake/ollama`, `insurance_claim_triage/ollama`, and `vendor_onboarding_risk/ollama` each terminalized `failed` after exhausting `3` repair cycles in generated-test lanes
- the service itself did not block; the runner completed and the remaining release-facing work is now the shared root cause across those three Ollama test failures
- a shared local remediation is now validated narrowly and against the exact persisted failed artifacts: the test autofix now lifts `required_fields` validators onto dict payload names before rebuilding positive-path fixtures, and local temporary pytest replays now pass for all three remaining Ollama scenarios
- a fresh focused Ollama-only rerun has now completed at `output/real_world_complex_matrix_2026_04_29_head03d6403_ollama_three_remaining_required_fields_autofix_rerun` for `kyc_compliance_intake`, `insurance_claim_triage`, and `vendor_onboarding_risk`; terminal output ended with exit `0` and emitted `campaign_summary.json` plus `campaign_summary.md`
- the active focused rerun has now fully recovered `kyc_compliance_intake/ollama` on a fresh process image: it completed at `2026-04-29T18:55:06.614122+00:00` with `repair_cycle_count=0`, `task_status_counts={done: 7}`, `run_result.status=completed`, and `acceptance_reason=all_evaluated_tasks_done`
- the same focused writer has now fully recovered `insurance_claim_triage/ollama` on a fresh process image: it completed at `2026-04-29T19:09:11.386074+00:00` with `repair_cycle_count=0`, `task_status_counts={done: 7}`, `run_result.status=completed`, and `acceptance_reason=all_evaluated_tasks_done`
- the same focused writer then completed `vendor_onboarding_risk/ollama` at `2026-04-29T19:25:20.307372+00:00` with `phase=completed`, `terminal_outcome=completed`, `repair_cycle_count=0`, and `task_status_counts={done: 7}` after `legal` completed at `2026-04-29T19:25:20.277421+00:00`
- the focused rerun therefore re-cleared all three previously failed canonical Ollama scenarios on a fresh process image; the next release-facing action is a fresh canonical full-scope rerun to verify the head returns from `12/15` to `15/15`
- after re-verifying the USB backup gate at `2026-04-29 20:30 WEST`, that fresh canonical full-scope rerun was launched in terminal `99772377-d519-4a2b-9dad-00d1c4023ecc` at `output/real_world_complex_matrix_2026_04_29_head03d6403_full_scope_post_required_fields_autofix_rerun`
- that new root has now fully cleared the `insurance_claim_triage` scenario family across all three providers, with `insurance_claim_triage/ollama` completing at `2026-04-29T20:07:05.176029+00:00` after `legal` completed at `2026-04-29T20:07:05.150703+00:00`
- `vendor_onboarding_risk/openai` and `vendor_onboarding_risk/ollama` completed cleanly, while `vendor_onboarding_risk/anthropic` remains failed with `run_result.status=execution_error`
- inside `returns_abuse_screening`, `openai` remains durably failed at `2026-04-29T20:28:28.485929+00:00` with `run_result.status=execution_error`, while both `anthropic` and `ollama` completed cleanly
- the rerun then completed the full `access_review_audit` family: `openai` and `anthropic` closed cleanly, and `access_review_audit/ollama` completed at `2026-04-29T21:11:06.935887+00:00` after one repair cycle with `code__repair_1` and `tests__repair_1`
- the canonical full-scope rerun is now fully terminalized at `13` completed cells and `2` `execution_error` cells, so release remains blocked only by `vendor_onboarding_risk/anthropic` and `returns_abuse_screening/openai`
- a local blocker-specific mitigation is now validated against the exact persisted failed test payloads embedded in those two blocker `project_state.json` files: the current autofix replaces vendor happy-path placeholders with concrete implementation literals and rewrites the returns `items` placeholder list into a concrete list-of-dicts payload, but fresh empirical rerun evidence is still required before either blocker can be cleared
- the fresh focused rerun at `output/real_world_complex_matrix_2026_04_29_head03d6403_non_ollama_blocker_literal_example_autofix_rerun` has now closed `4/4`: `vendor_onboarding_risk/openai`, `vendor_onboarding_risk/anthropic`, and `returns_abuse_screening/openai` all completed cleanly with `repair_cycle_count=0`, while `returns_abuse_screening/anthropic` completed at `2026-04-29T22:03:19.563229+00:00` with `repair_cycle_count=1` after a recovered `tests` repair lane
- that focused fresh-process bundle therefore empirically clears the two remaining non-Ollama blocker lanes from the canonical `13/15` rerun; the next release-facing action is a fresh canonical full-scope rerun to test whether the current head now reaches `15/15`
- the fresh canonical full-scope rerun is no longer on a clean path: `insurance_claim_triage/ollama` degraded at `2026-04-29T22:38:08.266714+00:00` with `task_status_counts={done: 3, failed: 1, skipped: 3}`, `tests` failed, downstream tasks were skipped, and `repair_cycle_count=0`; the earlier `kyc_compliance_intake/*` family and the network-backed lanes in `insurance_claim_triage/*` remain clean
- a local mitigation for that degraded lane is now validated before the next empirical rerun: `qa_tester`'s validation-failure normalizer now rewrites the persisted failure shape where `details=None` was already present but the generated test still asserted `is_valid is True` and then asserted on the unreachable `handle_request(...)` outcome; the focused `tests/test_qa_tester.py` slice passed with `4 passed, 195 deselected`
- the narrow proof rerun at `output/real_world_complex_matrix_2026_04_29_head03d6403_insurance_claim_triage_ollama_validation_failure_semantics_fix_rerun` then completed cleanly at `2026-04-29T23:19:51.383550+00:00` with `phase=completed`, `terminal_outcome=completed`, `repair_cycle_count=0`, and `task_status_counts={done: 7}`, converting the local `qa_tester` mitigation into fresh-process empirical evidence
- after crash recovery, the supposedly stale canonical rerun at `output/real_world_complex_matrix_2026_04_29_head03d6403_full_scope_post_non_ollama_blocker_literal_example_autofix_rerun` was found still live and already far beyond the old degraded lane: `insurance_claim_triage/ollama` had recovered to `completed` at `2026-04-29T22:51:17.623289+00:00` with `repair_cycle_count=1`, `vendor_onboarding_risk/openai` had completed at `2026-04-29T22:52:57.426253+00:00`, `vendor_onboarding_risk/anthropic` at `2026-04-29T22:54:50.518325+00:00` with `repair_cycle_count=1`, `vendor_onboarding_risk/ollama` at `2026-04-29T23:21:44.029079+00:00`, and `returns_abuse_screening/openai` at `2026-04-29T23:23:14.165098+00:00`
- the same live canonical root then shifted to a new blocker: `returns_abuse_screening/anthropic` terminalized `degraded` at `2026-04-29T23:25:05.093925+00:00` with `repair_cycle_count=3`, `task_status_counts={done: 4, failed: 5, skipped: 4}`, and `run_result.status=completed` with `acceptance_reason=evaluated_tasks_incomplete`
- the writer stayed alive after that degraded Anthropic lane and then completed `returns_abuse_screening/ollama` cleanly at `2026-04-29T23:39:35.102322+00:00` with `phase=completed`, `terminal_outcome=completed`, `repair_cycle_count=0`, and `task_status_counts={done: 7}`
- the same live canonical root then advanced into `access_review_audit/openai`, which completed at `2026-04-29T23:41:03.648071+00:00`, and `access_review_audit/anthropic`, which completed at `2026-04-29T23:42:40.214969+00:00`
- `access_review_audit/ollama` then cleared `code` and `deps` by `2026-04-29T23:49:32.144258+00:00` with no repair lane opened yet
- the same lane then failed `tests` at `2026-04-29T23:52:33.494277+00:00`, opening repair cycle `1`
- `access_review_audit/ollama` has now completed at `2026-04-30T00:05:37.695417+00:00` with `phase=completed`, `terminal_outcome=completed`, `repair_cycle_count=1`, and `task_status_counts={done: 9}`; `run_result.json` reports `status=completed` with `acceptance_reason=all_evaluated_tasks_done`
- the canonical full-scope root is now fully terminalized at `14` completed cells, `1` degraded cell, `0` active cells, and `0` unstarted cells
- the only remaining non-clean checkpoint on this root is `returns_abuse_screening/anthropic`, which had already terminalized `degraded` at `2026-04-29T23:25:05.093925+00:00`
- no writer remains on the canonical root; the only remaining release-facing blocker on this evidence bundle is the degraded `returns_abuse_screening/anthropic` lane
- a narrow current-head mitigation is now locally validated for that lane: `qa_tester` recognizes positive `result.risk_factors` assertions in generated `test_risk_scoring()` cases and rewrites returns-screening payloads onto the implementation's real risk-bearing schema keys (`prior_returns`, `receipt_present`, `items`); focused regressions passed, but fresh empirical recovery evidence is still required
- the same mitigation is now proven against the exact degraded artifact chain too: the persisted failed `tests` and `tests__repair_2` modules for `returns_abuse_screening/anthropic` both rewrite to schema-correct high-risk fixtures and both pass locally against the persisted implementation artifact; fresh empirical recovery evidence is still required
- a fresh focused rerun for `returns_abuse_screening/anthropic` then completed cleanly on the published remediation head `f0bbe59` at `output/real_world_complex_matrix_2026_04_30_headf0bbe59_returns_abuse_screening_anthropic_risk_payload_rerun`; its terminal persisted state landed at `2026-04-30T00:21:50.797912+00:00` with `phase=completed`, `terminal_outcome=completed`, `repair_cycle_count=0`, all seven canonical tasks `done`, and `run_result.acceptance_reason=all_evaluated_tasks_done`
- the remaining release-facing question for this evidence bundle is no longer whether `returns_abuse_screening/anthropic` can recover; it is whether a fresh canonical full-scope rerun now closes `15/15` on the same published head
- the latest post-thaw rerun snapshot remains in `output/real_world_complex_matrix_2026_04_29_head03d6403_rc_requal_thawed/`; `kyc_compliance_intake/openai`, `kyc_compliance_intake/anthropic`, `insurance_claim_triage/openai`, and `insurance_claim_triage/anthropic` have all completed cleanly there
- the first post-thaw Ollama scenario (`kyc_compliance_intake/ollama`) recovered its initial `code` validation failure, completed `deps`, failed `tests`, partially recovered repair cycle `2`, then exhausted its final repair budget when `tests__repair_3` also failed; the latest persisted scenario state is `phase=failed`, `terminal_outcome=failed`, `repair_cycle_count=3`, `updated_at=2026-04-29T00:30:39.817274+00:00`
- failure policy `continue` then advanced the same root beyond `insurance_claim_triage/ollama`, whose final persisted rewrites show `code__repair_1` recovered the repeated Ollama `code` defect and the scenario then completed cleanly in `phase=completed` with `7` tasks done, `repair_cycle_count=1`, and `updated_at=2026-04-29T01:17:32.154579+00:00`
- the same root then completed `vendor_onboarding_risk/openai` cleanly in `phase=completed` with `7` tasks done and `updated_at=2026-04-29T01:18:50.552353+00:00`
- the next persisted scenario, `vendor_onboarding_risk/anthropic`, completed with `phase=completed`, `terminal_outcome=degraded`, `repair_cycle_count=2`, and `updated_at=2026-04-29T01:21:27.177170+00:00`; all canonical tasks reached `done`, but a failed repair lane remains in the scenario history
- `vendor_onboarding_risk/ollama` then widened its generated-test repair lane once more: at persisted checkpoint `2026-04-29T02:13:54.273606+00:00`, the scenario sat at `phase=failed`, `terminal_outcome=failed`, and `repair_cycle_count=3`, with derived task state `done=4`, `failed=3`, `pending=4`; `tests__repair_2` failed with three pytest failures (`test_happy_path`, `test_validation_failure`, and `test_batch_processing`), and `tests__repair_3` appeared only as the next pending repair task in the persisted state
- terminal output then showed a local `KeyboardInterrupt` while the workflow was entering `tests__repair_3`, so no later persisted write ever landed for that pending repair task
- a subsequent whole-root `resume_failed` attempt was aborted after it re-executed `kyc_compliance_intake/openai` and rewrote that clean scenario state on the same output root; that restart is treated as an operator mistake, not a canonical new checkpoint for this bundle
- the later scenario-scoped relaunch limited to `vendor_onboarding_risk/ollama` also proved unsafe before the runner fix landed: it rebuilt the scenario from scratch and overwrote the prior failed state with a fresh `arch` checkpoint at `2026-04-29T02:25:22.749396+00:00` (`phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, `task_status_counts={done: 1, pending: 6}`)
- `scripts/run_real_world_complex_matrix.py` is now patched and locally validated so future resume-mode launches on existing roots reuse cached terminal results and load persisted failed/interrupted state instead of blindly rebuilding projects
- the current post-thaw root remains contaminated despite that fix, so any further empirical recovery for this head now requires a clean recovery root rather than another relaunch on the same path
- that clean recovery root is now a closed repository-owned old-runtime failure artifact at `output/real_world_complex_matrix_2026_04_29_head03d6403_vendor_ollama_postfix_recovery/`; its final persisted checkpoint landed at `2026-04-29T03:09:34.506579+00:00` with `phase=failed`, `terminal_outcome=failed`, `failure_category=workflow_blocked`, `repair_cycle_count=3`, and `task_status_counts={done: 1, failed: 4, pending: 5}`
- the clean root repeated the same Ollama `code_validation` defect across the initial `code` task plus `code__repair_1`, `code__repair_2`, and `code__repair_3`: all four failed with `Generated code validation failed: module import failed: NameError: name 'dataclass' is not defined`
- terminal output ended with scenario summary `status=execution_error phase=failed terminal=failed`, so no live writer remains on that root; because the process started before the local validation-runtime autofix landed, this closed chain remains old-runtime evidence only
- the fresh runtime-autofix rerun has now terminalized as a repository-owned degraded evidence root at `output/real_world_complex_matrix_2026_04_29_head03d6403_vendor_ollama_runtime_autofix_rerun/`; its terminal persisted checkpoint landed at `2026-04-29T03:47:18.018534+00:00` with `phase=completed`, `terminal_outcome=degraded`, `failure_category=scenario_validation`, `repair_cycle_count=0`, and `task_status_counts={done: 7}`
- that fresh-process terminal checkpoint proves `arch`, `code`, `deps`, `tests`, `review`, `docs`, and `legal` all completed with no repair lane opened; the earlier local validation-runtime autofix changed the old `code` failure outcome and allowed the rerun to converge through every workflow task on a new process image
- the persisted fresh-run artifacts now include `code_implementation.py`, `tests_tests.py`, `review_review.md`, `docs_documentation.md`, and `legal_legal_analysis.md`; `code_implementation.py` begins with `from dataclasses import dataclass`, matching the normalized runtime path rather than the missing-import output that blocked the old-runtime chain
- `run_result.json` for this root reports `status=validation_error`, `acceptance_reason=productivity_validation_failed`, and scenario-validation error `Generated code did not explicitly reject a malformed request through handle_request(...)`; additional checks for audit signal, batch processing, invalid request handling, and observable risk signal remained unmet, so the open blocker is now productivity/behavior validation rather than the old `code` validation defect
- a separate direct local validation had already confirmed that the exact historical failed `failed_artifact_content` from the closed old root passes the current `validate_code_output_runtime(...)` path after the runtime injects `from dataclasses import dataclass` and rewrites the output payload in memory; the old runtime defect is therefore closed for this evidence bundle
- a further fresh vendor-only rerun under additional malformed-request prompt/tests hardening has now terminalized as a repository-owned clean workflow-evidence root at `output/real_world_complex_matrix_2026_04_29_head03d6403_vendor_ollama_malformed_request_prompt_rerun/`; its terminal persisted checkpoint landed at `2026-04-29T04:30:25.694403+00:00` with `phase=completed`, `terminal_outcome=completed`, `failure_category=null`, and `task_status_counts={done: 7}`
- that terminal checkpoint proves `arch`, `code`, `deps`, `tests`, `review`, `docs`, and `legal` all completed with no repair lane opened, while the persisted `code_implementation.py` explicitly raises `ValueError("Invalid request details")` when `handle_request(...)` receives malformed non-dict `details`
- the persisted `tests_tests.py` artifact on the same root still shows the remaining semantic gap: its validation-failure case uses `details=None` and a permissive `try/except ValueError` fallback instead of explicitly asserting the malformed-request `ValueError` contract
- the current local validator now inspects generated test artifacts and rejects that completed root with `Generated tests did not explicitly assert ValueError for malformed non-dict details.`, so this terminal root is valid workflow-convergence evidence but not yet acceptance evidence for the latest validator slice
- the next fresh vendor-only rerun under the new generated-tests validator is now the active empirical frontier at `output/real_world_complex_matrix_2026_04_29_head03d6403_vendor_ollama_generated_test_validator_rerun/`; its first persisted checkpoint landed at `2026-04-29T04:39:30.539084+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and `task_status_counts={done: 1, pending: 6}`
- that checkpoint shows `arch` completed cleanly and terminal output already advanced into `code`, while only `artifacts/arch_architecture.md` exists on disk so far
- the same validator-aware rerun has now advanced to persisted checkpoint `2026-04-29T04:50:53.237700+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and `task_status_counts={done: 2, pending: 5}`; `arch` and `code` are done, terminal output already advanced into `deps`, and the persisted `code_implementation.py` still explicitly raises `ValueError("Invalid request details")`
- the same validator-aware rerun has now advanced again to persisted checkpoint `2026-04-29T04:51:31.885520+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and `task_status_counts={done: 3, pending: 4}`; `arch`, `code`, and `deps` are done, terminal output already advanced into `tests`, and the artifact set now includes `requirements.txt`
- the same validator-aware rerun has now advanced again to persisted checkpoint `2026-04-29T05:01:33.741905+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and `task_status_counts={done: 4, pending: 3}`; `arch`, `code`, `deps`, and `tests` are done, terminal output already advanced into `review`, and the artifact set now includes `tests_tests.py`
- that new `tests_tests.py` artifact still uses the permissive invalid-request pattern, and a direct local probe of the active root under the current validator already fails with `Generated tests did not explicitly assert ValueError for malformed non-dict details.`
- the same validator-aware rerun has now advanced again to persisted checkpoint `2026-04-29T05:07:59.846622+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and `task_status_counts={done: 6, pending: 1}`; `review` and `docs` are done, terminal output already advanced into `legal`, and the artifact set now includes `review_review.md` plus `docs_documentation.md`
- the same validator-aware rerun has now terminalized at persisted checkpoint `2026-04-29T05:11:21.308805+00:00` with `phase=completed`, `terminal_outcome=degraded`, `failure_category=scenario_validation`, and `task_status_counts={done: 7}`; `run_result.json` matches the earlier direct probe exactly with scenario-validation error `Generated tests did not explicitly assert ValueError for malformed non-dict details.`
- the nearest local rewrite path has now been identified and fixed in `kycortex_agents/agents/qa_tester.py`: the QA tester post-processor no longer rewrites `with pytest.raises(ValueError)` inside `test_validation_failure` when the implementation already raises `ValueError`, focused regressions passed locally, and a direct probe against the real terminal implementation preserved the explicit malformed-request assertion
- a new fresh vendor-only rerun is now live at `output/real_world_complex_matrix_2026_04_29_head03d6403_vendor_ollama_qa_tester_value_error_preservation_rerun`; its first persisted checkpoint landed at `2026-04-29T05:20:42.763595+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and `task_status_counts={done: 1, pending: 6}` while `code` became the live frontier and the artifact set contained only `arch_architecture.md`
- the same QA-tester-preservation rerun has now advanced again to persisted checkpoint `2026-04-29T05:32:06.328743+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and `task_status_counts={done: 2, pending: 5}` while `deps` became the live frontier and the artifact set grew to include `code_implementation.py`
- the same QA-tester-preservation rerun has now advanced again to persisted checkpoint `2026-04-29T05:42:48.740933+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and `task_status_counts={done: 4, pending: 3}` while `review` became the live frontier and the artifact set grew to include `requirements.txt` plus `tests_tests.py`
- that new `tests_tests.py` artifact now preserves the explicit malformed-request `with pytest.raises(ValueError)` assertion, and a direct local `_validate_generated_scenario(...)` probe of the active root returns `validated=True` with every scenario check passing
- the same QA-tester-preservation rerun has now advanced again to persisted checkpoint `2026-04-29T05:48:49.137408+00:00` with `phase=execution`, `terminal_outcome=null`, `repair_cycle_count=0`, and `task_status_counts={done: 6, pending: 1}` while `legal` became the live frontier and the artifact set grew to include `review_review.md` plus `docs_documentation.md`
- the same QA-tester-preservation rerun has now terminalized at persisted checkpoint `2026-04-29T05:52:11.957915+00:00` with `phase=completed`, `terminal_outcome=completed`, `failure_category=null`, and `task_status_counts={done: 7}`; `run_result.json` reports `status=completed`, `acceptance_reason=all_evaluated_tasks_done`, and a fully green scenario-validation block
- the earlier `kyc_compliance_intake/ollama` terminal failure still remains the strongest completed blocker artifact on this root because the authentic `vendor_onboarding_risk/ollama` failed snapshot was lost from disk before the runner fix landed
- because the full-scope `15/15` gate is still blocked, release-candidate review remains closed and this bundle is intentionally pre-canary only
- live checkpoint exports from `snapshot()` and `internal_runtime_telemetry()` have been collected only for repository-owned pre-canary checkpoints; no live-window evidence exists yet

## Evidence References

- Phase 16 operations guide: `../canary-operations.md`
- repository evidence-root rules: `../README.md`
- current candidate parity and checkpoint references: `environment-parity.md`, `provider-health.json`, `workflow-summary.json`, `internal-runtime-telemetry.json`
- retained validation artifacts and structured outputs:
  - `../../../output/real_world_complex_matrix_2026_04_28_full_scope/campaign_summary.json`
  - `../../../output/real_world_complex_matrix_2026_04_28_full_scope/ollama/project_state.json`
  - `../../../output/real_world_complex_matrix_2026_04_28_head03d6403_rc_requal/kyc_compliance_intake/openai/project_state.json`
  - `../../../output/real_world_complex_matrix_2026_04_28_head03d6403_rc_requal/kyc_compliance_intake/openai/artifacts/code_implementation.py`
  - `../../../output/real_world_complex_matrix_2026_04_28_head03d6403_rc_requal/kyc_compliance_intake/anthropic/project_state.json`
  - `../../../output/real_world_complex_matrix_2026_04_28_head03d6403_rc_requal/kyc_compliance_intake/anthropic/run_result.json`
  - `../../../output/real_world_complex_matrix_2026_04_29_head03d6403_vendor_ollama_runtime_autofix_rerun/vendor_onboarding_risk/ollama/project_state.json`
  - `../../../output/real_world_complex_matrix_2026_04_29_head03d6403_vendor_ollama_runtime_autofix_rerun/vendor_onboarding_risk/ollama/run_result.json`
  - `../../../output/real_world_complex_matrix_2026_04_29_head03d6403_vendor_ollama_runtime_autofix_rerun/vendor_onboarding_risk/ollama/artifacts/code_implementation.py`
  - `../../../output/real_world_complex_matrix_2026_04_29_head03d6403_vendor_ollama_malformed_request_prompt_rerun/vendor_onboarding_risk/ollama/project_state.json`
